# Streamlit Cloud requirements (cloud demo profile).
# camelot is kept for table extraction, but via opencv-python-headless (no GUI/GL
# system libs) so it installs WITHOUT apt/packages.txt. camelot 'stream' mode
# (borderless tables) needs no Ghostscript; 'lattice' skips gracefully if gs is absent.
streamlit>=1.40
pandas>=2.0
pydeck>=0.9.0
openpyxl>=3.1
pypdf>=5.0.0
pdfplumber>=0.11.0
camelot-py>=0.11.0
opencv-python-headless>=4.8
python-docx>=1.1.0
openai>=1.0.0
pymupdf>=1.24.0        # GPT-4o vision path: render PDF pages to images
Pillow>=10.0
fastembed>=0.3.0
